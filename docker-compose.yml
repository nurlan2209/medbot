services:
  medbot-api:
    build:
      context: ./server
    env_file:
      - ./server/.env
    environment:
      - HOST=0.0.0.0
    ports:
      - "3001:3001"
    restart: unless-stopped
    volumes:
      - ./server/uploads:/app/uploads
